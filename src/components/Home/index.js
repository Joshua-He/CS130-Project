import React, { Component } from 'react';

// Render the homepage based upon different user identities
// One for instructor, one for user
const HomePage = () => (
  <div>
    <h1>Home</h1>
  </div>
);

